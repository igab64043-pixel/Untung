# Node dependencies
node_modules/
.pnpm-store/

# Expo & EAS
.expo/
dist/
web-build/
*.apk
*.aab
*.ipa

# Native builds (generated)
android/
ios/

# Assets & LLM GGUF models (for large file handling advice)
# assets/models/*.gguf (uncomment if model > 100MB and managed via Git LFS / external storage)

# Environment & Local secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
local.properties

# Logs & OS temp
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem
